name: Django CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  DJANGO_SETTINGS_MODULE: "config.settings"  # путь к settings
  PYTHON_VERSION: '3.12'

jobs:
  # Job для тестирования
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_habitflow_db
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      # Database
      POSTGRES_HOST: localhost
      POSTGRES_PORT: 5432
      POSTGRES_DB: test_habitflow_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/test_habitflow_db

      # Redis
      REDIS_HOST: localhost
      REDIS_PORT: 6379
      REDIS_URL: redis://localhost:6379/0

      # Django
      DEBUG: "True"
      SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY || 'test-secret-key-for-ci-${{ github.run_id }}' }}
      ALLOWED_HOSTS: "localhost,127.0.0.1"

      # Security
      CSRF_TRUSTED_ORIGINS: "http://localhost"
      SECURE_SSL_REDIRECT: "False"

    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
        cache-dependency-path: '**/requirements*.txt'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libpq-dev gcc

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        # Для проверки кода (опционально)
        pip install black flake8 isort

    - name: Wait for services to be ready
      run: |
        echo "⏳ Waiting for PostgreSQL..."
        for i in {1..10}; do
          if pg_isready -h localhost -p 5432; then
            echo "✅ PostgreSQL is ready!"
            break
          fi
          echo "Attempt $i/10..."
          sleep 3
        done
        
        echo "⏳ Waiting for Redis..."
        for i in {1..10}; do
          if redis-cli -h localhost -p 6379 ping | grep -q PONG; then
            echo "✅ Redis is ready!"
            break
          fi
          echo "Attempt $i/10..."
          sleep 2
        done

    - name: Run database migrations
      run: |
        python manage.py makemigrations --check --dry-run
        python manage.py migrate --noinput

    - name: Run code quality checks (optional)
      run: |
        # Проверка форматирования с Black
        black --check --diff . || echo "⚠️ Black check failed (run 'black .' to fix)"
        
        # Проверка стиля с Flake8
        flake8 . --count --max-complexity=10 --max-line-length=88 --statistics || echo "⚠️ Flake8 found issues"
        
        # Проверка импортов с isort
        isort --check-only --diff . || echo "⚠️ Import sorting needed (run 'isort .')"

    - name: Run Django tests
      run: |
        python manage.py test --verbosity=2 --noinput --parallel

    - name: Check for migrations
      run: |
        python manage.py makemigrations --check --dry-run
        if [ $? -eq 0 ]; then
          echo "✅ No new migrations needed"
        else
          echo "❌ New migrations detected! Run 'python manage.py makemigrations'"
          exit 1
        fi

    - name: Collect static files (test)
      run: |
        python manage.py collectstatic --noinput --clear
        echo "✅ Static files collected"

  # Job для деплоя на сервер
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Deploy to Yandex Cloud
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_IP }}
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          set -e  # Выход при ошибке
          echo "🚀 Начинаем деплой..."
          
          cd ${{ secrets.DEPLOY_DIR }}
          
          # Обновляем код
          echo "📥 Тянем код..."
          git pull origin main
          
          # Виртуальное окружение
          echo "🐍 Активируем venv..."
          source venv/bin/activate
          
          # Зависимости
          echo "📦 Ставим зависимости..."
          pip install -r requirements.txt
          
          # Миграции
          echo "🔄 Мигрируем БД..."
          python manage.py migrate --noinput
          
          # Статика
          echo "📁 Собираем статику..."
          python manage.py collectstatic --noinput
          
          # Перезапуск
          echo "🔁 Перезапускаем сервисы..."
          sudo systemctl restart gunicorn
          sudo systemctl reload nginx
          
          echo "✅ Деплой завершен в $(date)"
          
          # Быстрая проверка
          sleep 2
          echo "Проверка:"
          curl -s -o /dev/null -w "HTTP: %{http_code}\n" http://localhost || echo "Проверь вручную"

  # Job для уведомлений (опционально)
  notify:
    runs-on: ubuntu-latest
    needs: [test, deploy]
    if: always()  # Всегда запускаем, даже если предыдущие jobs упали

    steps:
    - name: Deployment status
      run: |
        echo "Deployment Summary:"
        echo "Repository: ${{ github.repository }}"
        echo "Branch: ${{ github.ref }}"
        echo "Commit: ${{ github.sha }}"
        echo "Author: ${{ github.actor }}"
        echo "Run URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        
        # Можно добавить уведомления в Slack/Telegram/Email
        if [ "${{ job.status }}" = "success" ]; then
          echo "✅ All jobs completed successfully!"
        else
          echo "❌ Some jobs failed. Check the workflow run for details."
        fi